;; gc. low value = run gc so many time
(setq gc-cons-threshold 500000)

;; message log buffer
(setq message-log-max 40000)

;; mini buffer history
(setq hitory-length 1000)

;; kill ring
(setq kill-ring-max 4040)
